---
name: agenthud-remove
description: Unregister this agent session from AgentHUD
---

# AgentHUD Remove

Unregister this agent session from AgentHUD.

## Instructions

Run this exact command and nothing else:

```bash
agenthud remove
```

**IMPORTANT: `agenthud remove` is a CLI tool that handles EVERYTHING automatically — deletes the status file, removes the hook, and cleans up CLAUDE.md. Do NOT manually delete files, edit settings, or modify JSON. Just run the single command above and report its output.**
